#include <stdio.h>
#include <string.h>

int main()
{
    int t; // ✅ total test case সংখ্যা (কতগুলো শব্দ ইনপুট আসবে)
    scanf("%d", &t);

    // 🔁 এখন লুপ চালানো হবে প্রতিটা টেস্ট কেসের জন্য
    for (int i = 1; i <= t; i++)
    {
        char inputWord[100001]; // ✅ ইনপুট হিসেবে একটি স্ট্রিং (word) নেওয়া হবে
        scanf("%s", inputWord); // 👉 ইউজার থেকে ইনপুট নিচ্ছে

        int totalSmall = 0;   // ছোট হাতের অক্ষরের সংখ্যা
        int totalCapital = 0; // বড় হাতের অক্ষরের সংখ্যা
        int digits = 0;       // সংখ্যা (0–9)
        int other = 0;        // অন্যান্য ক্যারেক্টার যেমন ! @ # ইত্যাদি

        // 🔁 স্ট্রিং এর প্রতিটি ক্যারেক্টার চেক করা হচ্ছে
        for (int i = 0; i < strlen(inputWord); i++)
        {
            int c = inputWord[i]; // ক্যারেক্টারকে int হিসেবে নিচ্ছে (ASCII মান)

            // ✅ ছোট হাতের অক্ষর চেক: 'a' থেকে 'z'
            if ('a' <= c && c <= 'z')
            {
                totalSmall++;
            }

            // ✅ বড় হাতের অক্ষর চেক: 'A' থেকে 'Z'
            else if ('A' <= c && c <= 'Z')
            {
                totalCapital++;
            }

            // ✅ ডিজিট চেক: '0' থেকে '9'
            else if ('0' <= c && c <= '9')
            {
                digits++;
            }

            // ✅ অন্য যেকোনো ক্যারেক্টার (!@#$%^&* ইত্যাদি)
            else
            {
                other++;
            }
        }

        // 🔢 শেষে আউটপুট দিচ্ছে — বড় হাতের, ছোট হাতের, ও সংখ্যার মোট সংখ্যা
        printf("%d %d %d\n", totalCapital, totalSmall, digits);
    }

    return 0;
}
