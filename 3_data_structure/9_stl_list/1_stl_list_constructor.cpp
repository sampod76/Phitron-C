#include <bits/stdc++.h>

using namespace std;
int main()
{
    /* // list to list copy
        list<int> a = {1, 2, 3, 6, 5};
        list<int> a2copy(a);
         // or->  Range-based for loop -- all time use
        for (int value : a2copy)
        {
            cout << value << " ";
        }
    */

    /* // array to list copy
         int n = 3;
        int a[n] = {1, 2, 3};
        list<int> arrayCopy(a, a + n);
        for (int value : arrayCopy)
        {
            cout << value << " ";
        }
    */

    /*
        vector<int> a = {1, 2, 3};
         list<int> vCopy(a.begin(), a.end());

         // vector<int> a;
         // a.push_back(1);
         // a.push_back(2);
         // a.push_back(3);
         for (int value : vCopy)
         {
             cout << value << " ";
         }
    */
    // list<int> a(10, 3);
    // iterator to shiftin next value and d reference it and show value
    // for (auto it = a.begin(); it != a.end(); it++)
    // {
    //     cout << *it << " ";
    // }
    // cout << endl;
    // // or->  Range-based for loop -- all time use
    // for (int value : a2copy)
    // {
    //     cout << value << " ";
    // }
    return 0;
}