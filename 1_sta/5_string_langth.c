
#include <stdio.h>

int main()
{

    char st[12];
    scanf("%s", &st);
    // printf("%s", st);
    int count = 0;
    for (int i = 0; st[i] != '\0'; i++)
    {
        count++;
    }
    printf("%d", count);

    return 0;
}

/*
🔹 ধাপ ১: ইনপুট নেওয়া
char st[12];
scanf("%s", &st);


এখানে

char st[12]; মানে তুমি ১২টা অক্ষর ধারণ করতে পারবে এমন একটি character array তৈরি করেছো।

scanf("%s", &st); মানে কিবোর্ড থেকে একটা শব্দ (string) ইনপুট নিচ্ছো।
👉 এখানে & দেওয়া আসলে দরকার নেই, কারণ st নিজেই একটা address নির্দেশ করে।
সঠিকভাবে লেখা উচিত:

scanf("%s", st);

🔹 ধাপ ২: স্ট্রিং-এর শেষ চিহ্ন '\0'

C ভাষায় string-এর শেষে স্বয়ংক্রিয়ভাবে একটি null character ('\0') যোগ হয়।
এই '\0' ব্যবহার করেই বোঝা যায়, স্ট্রিং কোথায় শেষ হয়েছে।

উদাহরণ:
যদি তুমি ইনপুট দাও —

hello


তাহলে মেমোরিতে আসলে থাকবে:

h  e  l  l  o  \0

🔹 ধাপ ৩: দৈর্ঘ্য গণনা করা
int count = 0;
for (int i = 0; st[i] != '\0'; i++)
{
    count++;
}


এখানে for লুপটি একে একে প্রতিটি অক্ষর পরীক্ষা করে:

যতক্ষণ পর্যন্ত st[i] '\0' না হয়, লুপ চলবে।

প্রতি বার count++ করে গণনা বাড়ানো হবে।

উদাহরণ:
hello হলে:

i=0 → h → count=1
i=1 → e → count=2
i=2 → l → count=3
i=3 → l → count=4
i=4 → o → count=5
i=5 → '\0' → লুপ বন্ধ


শেষে count = 5।

🔹 ধাপ ৪: আউটপুট দেখানো
printf("%d", count);


এটি সংখ্যাটি প্রিন্ট করে — অর্থাৎ স্ট্রিংয়ের দৈর্ঘ্য।

✅ উদাহরণ রান:
Input:
Bangladesh

Output:
10


কারণ “Bangladesh”-এ মোট ১০টা অক্ষর আছে।

🔹 সংক্ষেপে সারাংশ:
ধাপ	কাজ
1️⃣	স্ট্রিং ইনপুট নেওয়া
2️⃣	null character ('\0') পর্যন্ত ঘোরা
3️⃣	প্রতি অক্ষর গণনা করা
4️⃣	cou
*/